Así se crea una rama local y remota:

```bash
git checkout -b branch_name
git push -u origin branch_name
```

Para eliminar:
```bash
git branch -d branch-name
git push origin --delete remote-branch-name
```